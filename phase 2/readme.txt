the idea is that if the cursor goes off of the like button, it will move around to detect when the "glove" cursor is activated, thus recalibrating. 

an objective c script acknowledging what that "current" state of the cursor is i.e. "NSCursor" will give this data
